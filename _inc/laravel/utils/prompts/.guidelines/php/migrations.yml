# Migration Rules
# Flat key-value reference

fk_loop_multiple: "Use foreach for multiple FK column creation/dropping"
fk_no_loop_single: "Do NOT use foreach for a single FK column"
trait_is_numeric_benefit: "IsNumericBenefit — scale calculation for monetary columns"
trait_has_nullable_audit: "HasNullableAuditColumns — nullable created_at/updated_at/deleted_at"
trait_has_audit_fields: "HasAuditFields — standardized created_by/updated_by/deleted_by"
trait_employee_connected: "EmployeeConnected — employee owner logic"
enum_frequency: "Frequency — billing/payment cycles with normalize()"
enum_calculation_base: "CalculationBase — benefit/deduction calculation basis with normalize()"
enum_deduction_type: "DeductionType — payroll deduction categories with normalize()"
enum_document_kind: "DocumentKind — document classification with normalize()"
enum_gender: "Gender — gender representations with normalize()"
enum_mime_type: "MimeType — accepted file MIME types with normalize()"
enum_salary_type: "SalaryType — salary structure types with normalize()"
enum_user_type: "UserType — application user roles with normalize()"
const_database: "DatabaseConstants (DC) — TABLE_* and COL_* for core schema"
const_users: "UsersConstants (UC) — user-related tables/columns"
const_companies: "CompaniesConstants (CC) — company-related tables/columns"
const_projects: "ProjectsConstants (PC) — project-related tables/columns"
const_bills: "BillsConstants (BC) — billing-related tables/columns"
created_by_no_null_delete: "created_by must NOT use nullOnDelete — audit trails must survive user deletion"
uuid_primary_keys: "All main entity tables use UUID PKs"
use_constants_for_names: "Always use constants for column and table name references"
