# CLI Commands — 2026-03-14

## PHPUnit

- `php -d memory_limit=512M vendor/bin/phpunit --no-coverage tests/Feature/UtilityTest.php`: targeted UtilityTest

## Git

- `git add -A && git commit -m "fix: resolve remaining UtilityTest failures — CoA integer code, eager-load, date handling"`: commit 283ccce
- `git add -A && git commit -m "chore: add .backup/ and .tmp/ dirs to gitignore"`: commit fed5d29
