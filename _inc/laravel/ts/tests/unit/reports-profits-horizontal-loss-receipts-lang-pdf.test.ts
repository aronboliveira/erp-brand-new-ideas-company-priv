/**
 * Jest unit test for reports-profits-horizontal-loss-receipts-lang-pdf (lang route).
 * Auto-generated by generate-lang-harness.cjs
 *
 * @jest-environment jsdom
 */

describe("reports-profits-horizontal-loss-receipts-lang-pdf", () => {
  beforeEach(() => {
    document.body.innerHTML = `
      <div id="app">
        <div id="content"></div>
        <div id="toast-container"></div>
      </div>
    `;
    (window as unknown as Record<string, unknown>).translations = {};
  });

  test("module can be imported without throwing", async () => {
    await expect(import("../../src/public/assets/js/routes/reports/profits/horizontal/loss/receipts/lang/pdf.ts")).resolves.toBeDefined();
  });

  test("window.translations is populated after import", async () => {
    await import("../../src/public/assets/js/routes/reports/profits/horizontal/loss/receipts/lang/pdf.ts");
    const t = (window as unknown as Record<string, unknown>).translations;
    expect(t).toBeDefined();
    expect(typeof t).toBe("object");
  });
});
