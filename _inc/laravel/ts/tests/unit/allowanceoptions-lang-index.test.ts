/**
 * Jest unit test for allowanceoptions-lang-index (lang route).
 * Auto-generated by generate-lang-harness.cjs
 *
 * @jest-environment jsdom
 */

describe("allowanceoptions-lang-index", () => {
  beforeEach(() => {
    document.body.innerHTML = `
      <div id="app">
        <div id="content"></div>
        <div id="toast-container"></div>
      </div>
    `;
    (window as unknown as Record<string, unknown>).translations = {};
  });

  test("module can be imported without throwing", async () => {
    await expect(import("../../src/public/assets/js/routes/allowanceOptions/lang/index.ts")).resolves.toBeDefined();
  });

  test("window.translations is populated after import", async () => {
    await import("../../src/public/assets/js/routes/allowanceOptions/lang/index.ts");
    const t = (window as unknown as Record<string, unknown>).translations;
    expect(t).toBeDefined();
    expect(typeof t).toBe("object");
  });
});
