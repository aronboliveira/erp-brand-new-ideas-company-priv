# CLI Commands — 2026-03-19

## PHPUnit
- `php -d memory_limit=512M vendor/bin/phpunit --no-coverage`: full suite

## Playwright
- `npx playwright test`: full e2e run

## Git
- `git add -A && git commit -m "cleanup on cache of testing libraries + adjustments to pytest markers + fix to phpunit failures + playwright rbacs tests + several adjustments to wiring of routes and controllers methods + start of filling of translations"`: commit 2560f6b
